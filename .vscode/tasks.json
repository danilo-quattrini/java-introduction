{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "",
            "args": ["javac StandardLibrary.java j--.exe ${fileBasenameNoExtension}.java java -jar jasmin.jar ${fileBasenameNoExtension} java ${fileBasenameNoExtension}"]
        }
    ]
}